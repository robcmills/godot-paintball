extends Node2D

#@onready var viewport: SubViewport = $SubViewport
#@onready var splat_sprite: Sprite2D = $SplatSprite

#func _ready() -> void:
	#splat_sprite.material.set_shader_parameter("mask_tex", viewport.get_texture())
